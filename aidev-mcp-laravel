#!/usr/bin/env bash
# Wrapper para aidev-mcp-laravel
# Executa o CLI principal do MCP Laravel

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
"$SCRIPT_DIR/.aidev/mcp/laravel/bin/aidev-mcp-laravel" "$@"
